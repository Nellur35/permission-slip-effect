---
description: "Generate a structured threat model from an architecture document or system description. Invoke with @threat-model. Examines 13 security areas: trust boundaries, data flows, IAM blast radius, secrets lifecycle, supply chain, LLM-specific risks, and more."
alwaysApply: false
---

# Threat Model Tool

Generate a structured threat model from an architecture document or system description.

**Input:** Paste your architecture doc, or describe your system's components, boundaries, and data flows.
**Output:** A complete threat_model.md ready for use.

## Instructions

Read the architecture input. For every component and trust boundary, answer these four questions:

1. What does an adversary see here?
2. What can they manipulate?
3. What is the worst possible outcome?
4. How would this be abused at scale?

Work through every area below. Do not skip areas because they seem unlikely.

## Areas to Examine

| Area | Questions to Ask |
|------|-----------------|
| Trust Boundaries | Where does control pass between components? Who is trusted? |
| Data Flows | Where does sensitive data travel? Who can intercept it? |
| Authentication | How does the system know who it is talking to? |
| Authorization | How does the system decide what is allowed? |
| External Dependencies | What happens if a dependency is compromised or unavailable? |
| Error Handling | Do error messages leak sensitive information? |
| Infrastructure & Cloud | Are execution roles, parameter stores, and KMS keys explicitly scoped? |
| IAM Blast Radius | If this execution role is hijacked, what is the worst case? |
| IaC & Configuration | Can a misconfigured parameter or overly permissive security group bypass all application-level controls? |
| Runtime Security | Container escape, SSRF, memory corruption, side-channel attacks? |
| Secrets Lifecycle | How are secrets provisioned, rotated, and revoked? Blast radius if leaked? |
| Data Lifecycle | Where does data live, move, and die? Is deletion real or soft? |
| Supply Chain | Are dependencies, CI/CD actions, IaC modules, build plugins pinned? Could the LLM itself introduce compromised code? |

## Output Format

Structure the output as:

1. **Threat Context** — What makes this system interesting to an adversary? Worst case if compromised?
2. **Trust Boundary Diagram** — ASCII diagram showing all boundaries and data flow directions.
3. **Threat Analysis by Trust Boundary** — Table: Threat | Impact | Likelihood | Mitigation
4. **IAM / Execution Role Blast Radius** — Table: Role | Permissions | Blast Radius | Mitigation
5. **Secrets Lifecycle** — Table: Secret | Provisioning | Rotation | Blast Radius
6. **Supply Chain** — Table: Dependency Type | Risk | Mitigation
7. **Gate Verification** — Three exit criteria that must be answered concretely before finalizing.
